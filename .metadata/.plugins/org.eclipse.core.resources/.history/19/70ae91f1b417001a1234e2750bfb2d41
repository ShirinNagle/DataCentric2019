package com.shops;

import java.util.ArrayList;

import org.apache.tomcat.jni.User;
import org.bson.Document;

import com.google.gson.Gson;
import com.mongodb.MongoClient;
import com.mongodb.client.FindIterable;
import com.mongodb.client.MongoCollection;
import com.mongodb.client.MongoDatabase;

public class MongoDAO {
	
	String mongoDB = "storeHeadOfficeDB";
	String mongoCollection = "storeHeadOffice";
	
	MongoClient mongoClient;
	MongoDatabase database;
	MongoCollection<Document> collection;
	
	
	/* ======================================================================================================
	 * Constructor
	 * ====================================================================================================== */
	public MongoDAO() throws Exception {
		mongoClient = new MongoClient();
		database = mongoClient.getDatabase(mongoDB);
		collection = database.getCollection(mongoCollection);
	}
	
	public ArrayList<HeadOffice> loadHeadOffices(){
		FindIterable<Document> stores = collection.find();
		ArrayList<HeadOffice> headOffice = new ArrayList<HeadOffice>();
		Gson gson = new Gson();
		
		//Need to tidy up and figure out what is happeneing with this section
		/*for (Document d : stores) {
			User ho = gson.fromJson(d.toJson(), User.class);
			   headOffice.add(ho);*/

			//System.out.println(d.toJson());
		mongoClient.close();
			
			return headOffice;
			
		
		
	}
	
	
	
}//end of class
